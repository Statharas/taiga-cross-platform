# TODO and unfinished-screen audit

This audit separates user-facing unfinished screens from internal `@TODO`
comments. It was generated by scanning `src` for `TODO`, `not implemented`,
`placeholder`, and related strings, then mapping them back to visible UI.

## User-facing unfinished surfaces

| Area | Evidence | User impact | Requirement link |
| --- | --- | --- | --- |
| Search submit live fixtures | `taiga_sync::searchTitle()` routes to MAL/Kitsu/AniList search endpoints. | Backend search exists for all three services; live service tests still need authenticated/network fixtures. | Search submit requirement. |
| Torrent client launch/live feeds | Settings supports multi-condition structured filters, all/any matching, anime-ID limits, operator options, and discard hide/deactivate behavior. | Custom client path/app mode and broader real-feed fixtures still need live Linux workflow validation. | Torrent downloads and feed requirements. |
| Update workflow | Update dialogs exist in v1; v2 now exposes a cross-platform release-page check. | It does not yet download/apply updates like the original updater. | Auxiliary update dialog requirement. |

## Platform/backend TODOs that affect visible behavior

| Area | Evidence | User impact |
| --- | --- | --- |
| Certificate revocation/no-revoke | Proxy settings are applied through Qt networking. | Certificate revocation/no-revoke behavior still needs Qt/Linux mapping. |
| DDoS/Cloudflare error handling | `anilist.cpp` notes DDoS protection TODO; torrent docs now note TokyoToshokan `525`. | Some service/feed failures may be reported generically. |
| User-defined title priority | `track/recognition_cache.cpp` TODO. | Recognition may miss user aliases that v1 handled. |
| Queueing edits | `anime_list_model.cpp` TODO add-to-queue behavior. AniList direct save is implemented. | Offline/deferred sync parity is incomplete. |
| v1 history compat | `compat/history.cpp` TODO. | History import may miss fields. |

## Internal TODOs with lower direct screen impact

| Area | Evidence | Notes |
| --- | --- | --- |
| AniList viewer rating system | `anilist.cpp` TODO. | Affects settings/list score display once implemented. |
| AniList parser next airing episode | `anilist_parsers.cpp` TODO. | Would improve season/list metadata. |
| Debug/drop test path | v1 debug-only drop handling not ported. | Not a normal user workflow. |

## Immediate recommended backlog

1. Map certificate revocation/no-revoke behavior for Qt/Linux before deeper feed/service debugging.
2. Add authenticated/live-service fixtures for MAL, Kitsu, AniList, and torrent feed fallbacks.
3. Implement offline/deferred edit queueing for non-AniList save paths.
4. Validate custom torrent client launch paths on Linux.
